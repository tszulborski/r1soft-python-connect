# r1soft-python-connect
Python connector for R1Soft Server Backup Manager. It can recover files and MySQL databases from the backup server.

cdp.py - connector class
recover.py - example that shows how to use it
